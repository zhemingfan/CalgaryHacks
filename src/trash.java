import org.jsoup.Jsoup;
import org.jsoup.nodes.Document;
import org.jsoup.nodes.Element;

public class trash {
	

    //final String url = "https://sharestobeclosed.telegraph.co.uk/indices/financials/index/MCX";
    /*
	
	final String url = "http://contacts.ucalgary.ca/info/cpsc/contact-us/directory";
    try {

    //###uofc-table-1 > tbody > tr.odd:nth-of-type(1) > .uofc-directory-name-cell
    //###uofc-table-1 > tbody > tr.even:nth-of-type(2) > .uofc-directory-name-cell
        final Document document = Jsoup.connect(url).get();
        //System.out.println(document.outerHtml());
        
        
        //table class, since its table -> do table. instead of div. (if it were still)
        //also note that it should be .uofctable instead of _ u of c table (no spaces should be there)
        //tr for row
        
        
        
        for (Element row : document.select("table.unitis-person-list.uofctable tr")) {
            System.out.println("hi");
            
            row = document.select("###uofc-table-1 > tbody > tr:nth-child(" + i + ")");
        	
        }
    }
    catch (Exception ex) {
        ex.printStackTrace();
    }
	*/
//}
}
